# Obsidian 配置(主题、插件、CSS 片段等)入库跟着 git 走两端同步;
# 只排除纯设备本地状态:窗口布局/打开的标签页(两端各自不同,同步了只会
# 产生噪音提交甚至冲突)和缓存(体积大、无同步价值)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache/

# Claudian 插件的本地配置与会话缓存,不是记忆内容,不入库
claudian-settings.json
conv-*.json
conv-*.jsonl

# macOS 系统文件
.DS_Store
