---
cssclasses: workbench
---

```dataviewjs
// —— 横幅:日期 + 实时时钟 ——
const banner = dv.container.createEl("div", { cls: "wb-banner" });
const dateEl = banner.createEl("div", { cls: "wb-date" });
const clockEl = banner.createEl("div", { cls: "wb-clock" });
banner.createEl("div", { cls: "wb-sub", text: "记 忆 库 · 每 日 回 路" });
const wd = ["周日", "周一", "周二", "周三", "周四", "周五", "周六"];
const tick = () => {
  const d = new Date();
  dateEl.setText(`${d.getFullYear()} 年 ${d.getMonth() + 1} 月 ${d.getDate()} 日 · ${wd[d.getDay()]}`);
  clockEl.setText(`${String(d.getHours()).padStart(2, "0")}:${String(d.getMinutes()).padStart(2, "0")}`);
};
tick();
setInterval(tick, 15000);

// —— 统计卡片 ——
const ghost = {};
for (const links of Object.values(app.metadataCache.unresolvedLinks))
  for (const [t, n] of Object.entries(links))
    if (t.startsWith("concepts/")) ghost[t] = (ghost[t] || 0) + n;
const cards = [
  ["📝", dv.pages('"notes"').length, "笔记"],
  ["🔮", dv.pages('"concepts"').length, "概念"],
  ["🧭", dv.pages('"decisions"').length + dv.pages('"pitfalls"').length + dv.pages('"insights"').length, "决策·坑·洞察"],
  ["👻", Object.keys(ghost).length, "幽灵概念"],
];
const grid = dv.container.createEl("div", { cls: "wb-cards" });
for (const [icon, num, lbl] of cards) {
  const card = grid.createEl("div", { cls: "wb-card" });
  card.createEl("div", { cls: "wb-num", text: `${icon} ${num}` });
  card.createEl("div", { cls: "wb-lbl", text: lbl });
}
```

> [!draw]+ 🎴 今日抽卡 · 对抗遗忘
> [[notes/社会认知/人设的反作用|人设的反作用]] —— 07-02 录入。
>
> 先别点开,自测两题:
>
> 1. 文中说"人设经营你"和"账号运营你"是同一套机制在两个层面的翻版,分别对应哪两节的标题?
> 2. "功能性吸引"和"复杂性吸引"的对照表列了三个维度,除了传播效率,另外两个是什么?
>
> 想不起来再点进去对答案。

> [!seed]+ 🌱 今日延伸 · 一个新知识点
> 关于 [[concepts/主体性|主体性]]:心理学的**自我决定论**(Self-Determination Theory,Deci & Ryan)把"自主"(autonomy)列为三大基本心理需要之一——但他们定义的自主不是"为所欲为",而是"行为出自你真心认同的自我",哪怕这个认同只是一句"我不要"。[事实,推论部分是与消极主体性的类比,未联网核查]
>
> 这给"消极主体性先行"提供了另一层背书:光是稳定地说不,这个动作本身就已经够格算自主,不用等凑出一句宏大的"我要什么"才作数。

> [!ask]+ ❓ 今天的问题
> [[concepts/控制二分法|控制二分法]]的开放问题里有一条:它会不会被滥用成回避的借口——把其实可控(只是难)的事划进"不可控"提前放弃?
>
> 具体到你自己:最近有没有一件事,你心里知道"其实使劲还是能推一把",但因为太难,你选择把它归类成"不归我控制"来放过自己?
>
> > ✍️ 你的回答写在这里,下次生成时 AI 会读到。

> [!recent]+ 📥 最近输入(自动)
> ```dataview
> TABLE WITHOUT ID file.link AS 笔记, created AS 录入日期
> FROM "notes"
> SORT created DESC
> LIMIT 5
> ```

> [!ghost]+ 👻 幽灵概念(自动)
> 被笔记链到、但还没建页的概念;命中 ≥2 的会在每周 GC 时升格为实体页:
>
> ```dataviewjs
> const un = app.metadataCache.unresolvedLinks;
> const counts = {};
> for (const links of Object.values(un)) {
>   for (const [t, n] of Object.entries(links)) {
>     if (t.startsWith("concepts/")) counts[t] = (counts[t] || 0) + n;
>   }
> }
> const rows = Object.entries(counts).sort((a, b) => b[1] - a[1]);
> if (rows.length) dv.list(rows.map(([t, n]) => dv.fileLink(t) + " ×" + n));
> else dv.paragraph("暂无——所有被链概念都已建页。");
> ```
