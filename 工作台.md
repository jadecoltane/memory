---
cssclasses: workbench
generated: 2026-07-04
---

```dataviewjs
// —— 横幅:日期 + 实时时钟 ——
const banner = dv.container.createEl("div", { cls: "wb-banner" });
const dateEl = banner.createEl("div", { cls: "wb-date" });
const clockEl = banner.createEl("div", { cls: "wb-clock" });
banner.createEl("div", { cls: "wb-sub", text: "记 忆 库 · 每 日 回 路" });
const wd = ["周日", "周一", "周二", "周三", "周四", "周五", "周六"];
const tick = () => {
  const d = new Date();
  dateEl.setText(`${d.getFullYear()} 年 ${d.getMonth() + 1} 月 ${d.getDate()} 日 · ${wd[d.getDay()]}`);
  clockEl.setText(`${String(d.getHours()).padStart(2, "0")}:${String(d.getMinutes()).padStart(2, "0")}`);
};
tick();
setInterval(tick, 15000);

// —— 心跳:超过 48 小时没重新生成,说明每日任务可能挂了 ——
const gen = dv.current().generated;
const genMs = gen?.toMillis ? gen.toMillis() : (gen ? new Date(String(gen)).getTime() : NaN);
const ageH = isNaN(genMs) ? Infinity : (Date.now() - genMs) / 36e5;
if (ageH > 48) banner.createEl("div", {
  cls: "wb-sub",
  text: `⚠️ 工作台已 ${isFinite(ageH) ? Math.floor(ageH / 24) + " 天" : "很久"}没更新,每日任务可能没在跑`,
  attr: { style: "color:#e24b4a;" },
});

// —— 快捷入口 ——
const chips = dv.container.createEl("div", { cls: "wb-chips" });
const open = (path) => app.workspace.openLinkText(path, "/", false);
const chip = (label, fn) => { const b = chips.createEl("button", { cls: "wb-chip", text: label }); b.onclick = fn; };
chip("📇 记忆索引", () => open("index.md"));
chip("📋 结论速览", () => open("SUMMARY.md"));
chip("🎲 随机重逢一篇旧笔记", () => {
  const arr = dv.pages('"notes"').array();
  if (arr.length) open(arr[Math.floor(Math.random() * arr.length)].file.path);
});

// —— 统计卡片 ——
const notes = dv.pages('"notes"'), concepts = dv.pages('"concepts"');
const edges = [...notes.array(), ...concepts.array()].reduce((n, p) => n + p.file.outlinks.length, 0);
const weekNew = notes.array().filter(p => {
  const t = p.created?.ts ?? (p.created ? new Date(String(p.created)).getTime() : NaN);
  return !isNaN(t) && Date.now() - t < 7 * 864e5;
}).length;
const cards = [
  ["📝", notes.length, "笔记"],
  ["🔮", concepts.length, "概念"],
  ["🧭", dv.pages('"decisions"').length + dv.pages('"pitfalls"').length + dv.pages('"insights"').length, "决策·坑·洞察"],
  ["⏳", notes.array().filter(p => p.verified === false).length, "待复核"],
  ["🕸️", edges, "双链"],
  ["🌱", weekNew, "本周新增"],
];
const grid = dv.container.createEl("div", { cls: "wb-cards" });
for (const [icon, num, lbl] of cards) {
  const card = grid.createEl("div", { cls: "wb-card" });
  card.createEl("div", { cls: "wb-num", text: `${icon} ${num}` });
  card.createEl("div", { cls: "wb-lbl", text: lbl });
}
```

## ☀️ 每日回路

> [!draw]+ 🎴 今日抽卡 · 对抗遗忘
> [[notes/主体性/破解东亚绩优主义疲惫感的五个哲学解法|破解东亚绩优主义疲惫感的五个哲学解法]] —— 07-04 录入。
>
> 先别点开,自测两题:
>
> 1. 文中给出了五个哲学解法,分别对应东亚绩优主义的哪五个痛点(比如"找最优解""模糊人际边界"……)?试着一一对应,不用记术语原名。
> 2. 文中把阿德勒的"课题分离"称为"控制二分法的人际版"——为什么这两件事其实是同一个逻辑在两个场景里的应用?
>
> 想不起来再点进去对答案。

> [!seed]+ 🌱 今日延伸 · 一个新知识点
> 关于 [[concepts/主体性|主体性]]:今天新整理的笔记里引用了"存在先于本质"这句话——出自萨特 1946 年的演讲《存在主义是一种人道主义》。他的原意是:人不像剪刀一样带着预设的"功能说明书"出厂,人先存在,然后靠自己的选择去定义自己是谁。[事实]
>
> 这给"把自己当主体、不当客体"提供了最早的哲学出处:把自己当客体去打造(光宗耀祖、完美简历),本质上是先默认接受了一份别人写好的"说明书",再拿它打分。主体性的做法,是拒绝这份说明书本身,而不是想办法把分数打高。[推论,基于萨特原意与"主体 vs 客体"这条解法的类比,未联网核查]

> [!ask]+ ❓ 今天的问题
> [[concepts/主体性|主体性]]的开放问题里有一条:消极主体性(先学会说"我不要")练熟之后,怎么知道该往积极主体性("我要什么")切换了?有没有信号?
>
> 具体到你自己:回想一件你曾经真的想清楚"我要什么"的事——那种笃定感是怎么冒出来的?是攒够了"我不要"之后自然浮现,还是靠别的什么触发的?

> [!note]- 💬 我的看法（先答完再展开）
> 我倾向于认为切换没有一个清晰的"信号时刻",更像是回头才发现的。[观点]
>
> 消极主体性的作用是清场——把不属于你的东西一件件剔除。剔到某个密度之后,剩下的空白里自然会有某个方向显得比其他方向"更安静"——不是激动,而是一种不需要解释的省力感。这就是积极主体性开始成形的状态。[推论]
>
> 所以"信号"可能不是一个新的冲动冒出来,而是当你面对某件事时,说"不要"变得困难了——那个困难本身就是信号:这里有你真的想要的东西在抵抗被排除。[推论,未联网核查]

✍️ **你的回答：**



---

## 📰 外部推送

> [!feed]+ 📰 今日推送 · 2026-07-05
> 1. [Exploring top automotive HMI trends for 2026](https://star.global/posts/automotive-hmi-trends/) — Star · 行业综述:多模态(语音/触控/手势/视线)取代实体按键成主线,软件定义座舱把仪表、HUD、氛围灯拉进同一套体验——对照你在做的 HMI 方向。
> 2. [Actions, Speech, and Looks: What Shapes How We Feel About In-Vehicle AI Assistants?](https://dl.acm.org/doi/10.1145/3772318.3790435) — CHI 2026 论文 · 大样本视频调研车内 AI 助手:用户明确反感机器人拟人形象和过高自主性,"更智能"不等于"更讨喜"——可以直接用进作品集的设计论证。
> 3. [The algorithmic self: how AI is reshaping human identity, introspection, and agency](https://www.ncbi.nlm.nih.gov/pmc/articles/PMC12289686/) — 学术长文 · 算法投喂如何反过来塑造自我认知与能动性,和你 notes/社会认知/人设的反作用 正面呼应。
>
> 链接来自当日 WebSearch 实时结果,每天覆盖不留存;想留哪篇,说「记下来」。

## 🗂️ 笔记库

```dataviewjs
// —— 笔记库:按分类的卡片墙(实时) ——
const pages = dv.pages('"notes"').array();
const groups = new Map();
for (const p of pages) {
  const seg = p.file.folder.split("/");
  const cat = seg.length > 1 ? seg[1] : "未分类";
  if (!groups.has(cat)) groups.set(cat, []);
  groups.get(cat).push(p);
}
for (const [cat, list] of [...groups.entries()].sort((a, b) => b[1].length - a[1].length)) {
  list.sort((a, b) => (b.created?.ts ?? 0) - (a.created?.ts ?? 0));
  const sec = dv.container.createEl("div", { cls: "wb-lib-group" });
  const head = sec.createEl("div", { cls: "wb-lib-head" });
  head.createEl("span", { cls: "wb-lib-cat", text: cat });
  head.createEl("span", { cls: "wb-lib-count", text: String(list.length) });
  const grid = sec.createEl("div", { cls: "wb-lib-grid" });
  for (const p of list) {
    const card = grid.createEl("a", { cls: "wb-lib-card" });
    card.onclick = () => app.workspace.openLinkText(p.file.path, "/", false);
    card.createEl("div", { cls: "wb-lib-title", text: p.file.name });
    const meta = card.createEl("div", { cls: "wb-lib-meta" });
    meta.createEl("span", { text: p.created?.toFormat ? p.created.toFormat("MM-dd") : "—" });
    if (Date.now() - (p.created?.ts ?? 0) < 7 * 864e5) meta.createEl("span", { cls: "wb-lib-new", text: "新" });
    if (p.verified === false) meta.createEl("span", { cls: "wb-lib-badge", text: "待复核" });
  }
}
```
